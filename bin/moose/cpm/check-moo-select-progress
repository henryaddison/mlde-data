#!/bin/bash

for d in /gws/nopw/j04/furflex/henrya/projects/furflex/data/moose-experiments/1981-2000-pSTW-cpm/mi-bb*/*/data; do
  echo ${d};
  ls ${d}/*.pp | wc -l;
  ls -Ssh ${d}/*.pp | head -n 2;
  ls -Ssh ${d}/*.pp | tail -n 2;
done
