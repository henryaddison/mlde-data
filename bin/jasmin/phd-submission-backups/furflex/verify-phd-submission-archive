#!/bin/bash
# Wrapper script around backing up phd submission files to be run on LOTUS2

#SBATCH --job-name="verify PhD backup"
#SBATCH --account=furflex
#SBATCH --partition=standard
#SBATCH --qos=standard
#SBATCH --mem=8G
#SBATCH --time=23:00:00

set -euo pipefail

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

set -x

cd /gws/nopw/j04/furflex/henrya/phd-submission/workdirs
tar --compare --file=workdirs.tar $(find * -maxdepth 0 -type d)
gzip workdirs.tar
echo "Done workdirs"

cd /gws/nopw/j04/furflex/henrya/phd-submission/moose
tar --compare --file=moose.tar $(find * -maxdepth 0 -type d)
gzip moose.tar
echo "Done moose"

set +x

echo Done
